# Supabase 配置
# 在 Supabase 项目后台：Project Settings → API
#   - Project URL  → VITE_SUPABASE_URL
#   - anon public  → VITE_SUPABASE_ANON_KEY
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-public-key

# 说明：
# 1. 本地开发：复制本文件为 .env.local 并填入真实值
# 2. Vercel 部署：在项目 Environment Variables 中添加这两个变量
# 3. 不配置时应用仍可运行（使用内置 mock 数据，不进行云同步）
